# Social Automation Studio — backend deps.
# NOTE: a few pins differ from the original CLAUDE.md spec for Python 3.12 +
# Pillow 10 compatibility (moviepy 1.0.3 calls the removed Image.ANTIALIAS and
# will not import alongside Pillow>=10; we use ffmpeg directly and moviepy 2.x).

# --- Web / async ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9
aiofiles==23.2.1
httpx==0.27.0

# --- DB ---
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9      # PostgreSQL on Railway

# --- Queue / schedule ---
celery==5.4.0
redis==5.0.4
apscheduler==3.10.4

# --- LLMs ---
groq==0.8.0
google-generativeai==0.7.0
ollama==0.2.1

# --- TTS / audio ---
edge-tts>=7.0.0           # >=7 required: 6.x lacks the WordBoundary boundary option
gtts==2.5.1
pydub==0.25.1
librosa==0.10.2
mutagen==1.47.0

# --- Video / image ---
# NOTE: we render with FFmpeg directly (more reliable than MoviePy, which has a
# Pillow>=10 ANTIALIAS incompatibility). MoviePy intentionally omitted.
ffmpeg-python==0.2.0
pillow>=10.3.0
yt-dlp>=2024.5.27

# --- Platform APIs ---
google-api-python-client==2.131.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.4.0
pytrends==4.9.2

# --- Config / crypto ---
cryptography==42.0.7
python-dotenv==1.0.1
pydantic-settings==2.2.1
